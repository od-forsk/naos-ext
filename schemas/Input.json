{
  "description": "Process execution input payload (compliant with unit task definition input JSON schema).",
  "type": "object",
  "format": "application/json",
  "$schema": "http://json-schema.org/schema#"
}